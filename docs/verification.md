# 実装・検証記録

## v0.0.0.3 Beta（2026-09-20）

アーキテクチャレビューに基づく改修。会話／整理依頼の入口は明示選択、履歴の自動削除は行わない方針を採用しました。旧測定と今回の測定は区別します。

- 型検査・書式検査成功。自動試験112件中111成功、1件スキップ（NTFSが8.3短縮名を返さない既存環境条件）。設定保存中の表示・選択変更、保存中断、通知故障とジャーナルの分離、通知の欠落・重複、保存データ検証、トランザクションのロールバック、workerのバックアップ・削除・画像上限を追加検証。
- 実UIで、通常会話に意図判定を呼ばないこと、整理を頼む文章でも会話モードでは計画を作らないこと、整理依頼モードで計画を作ること、承認後の移動と復元、VRM 0.x/1.0のインポートを確認。
- 配布EXEでSSE停止・応答上限・VRM切替・workerバックアップ・会話削除・非表示状態の再起動保持を確認。WindowsのFileVersion／ProductVersionはともに0.0.0.3。
- 旧リリース時の隔離テストデータを複製し、新配布EXEで1会話・2計画・2アバターと操作内容の保持を確認。利用者の通常データは検証に使っていません。
- 模擬ストリーム中の25回の実DOM入力・タブ切替・停止で、イベント受領からDOM更新後rAFまでp95 17.4ms、最大17.7ms。軽量VRMと模擬サーバーの短時間測定であり、実LLM負荷や物理入力の遅延ではありません。

### 履歴増加時の比較

同じ件数の合成データ（80会話、8,000メッセージ、100計画、20,000候補）を隔離した実Electronで各5回測定しました。

| 指標 | 改修前のローカルビルド | 改修後 |
| --- | --- | --- |
| 表示切替のイベントJSONサイズ | 12,709,223 bytes | 約942〜943 bytes |
| 表示切替APIの往復時間 | 147.4〜174.9ms | 20.3〜28.7ms |
| 明示的な全状態取得API | 162.9〜179.0ms | 82.6〜97.0ms |
| バックアップAPIの完了時間 | 39.3ms（Main内同期） | 117.4ms（worker起動を含む） |

イベントサイズはUTF-8 JSON換算でChromiumの実転送量ではありません。API時間はrendererのイベント処理とIPCを含みます。バックアップは総完了時間の短縮ではなくMainの応答性維持が目的です。改修後のバックアップ中にMainの10msタイマーは7回動作し、最大間隔19msでした。小さい標本・単一PCの結果です。

### 証跡と再実行

| 証跡 | 内容 |
| --- | --- |
| artifacts/refactor-tests.txt | 112件の自動テスト結果 |
| artifacts/ui-1789862174781/ | 通常会話／整理依頼分岐、移動・復元、VRM両世代 |
| artifacts/acceptance-1789862456896/ | 新配布EXEでの受け入れ試験 |
| artifacts/ui-1789862547725/ | 新配布EXEでの会話／整理依頼・移動・復元・VRM両世代 |
| artifacts/upgrade-1789862456856/ | 旧版のテストデータの引継ぎ |
| artifacts/state-benchmark-1789861341339/result.json | 改修前の負荷比較 |
| artifacts/state-benchmark-1789862257235/result.json | 改修後の負荷比較・Mainタイマー・bundle SHA-256 |
| artifacts/responsiveness-1789862200398/result.json | 模擬ストリーム中25操作の応答性 |

`node scripts/benchmark-state.mjs`で隔離データを生成して再測定できます。`COMPANION_PACKAGE`にEXEパスを指定すると`scripts/acceptance.mjs`と`scripts/smoke.mjs`を配布版で実行できます。`scripts/verify-upgrade.mjs <旧版の停止済みテストデータフォルダー>`は原本を変更せずコピーで検証します。

最初の応答性試験は測定器が旧`state`イベントのみを参照して失敗しました。`update`にも対応した後、上記の25操作を完走しています。梱包時のElectron取得はネットワークタイムアウトがあったため、公式SHASUMS256.txtとSHA-256一致を確認したキャッシュZIPを使用しました（790a355b684d5c7cc8dc3cdd8c4cca7c4b2d054685427c7554a956879a82e70b）。

起動・再同期時の全件読込み、通常DB読書きの同期処理、巨大履歴でのメモリ使用は残課題です。2時間表示試験、1GB目標、混在DPI・複数画面など従来未確認の受け入れ条件は未完了のままです。

## 過去の検証記録

追加要望への対応（モーション、ドラッグ表示、GGUF切替、メモリ解放）と実機測定は[改善記録](improvements.md)に追記しています。以下のMVP当時の測定値と、今回の測定値は区別します。

v0.0.0.2 Betaの配布用EXEは再ビルド後、`artifacts/acceptance-1789829866012/`で外部LLM経路・SSE停止・保存・VRM切替・非表示の再起動保持を確認しました。WindowsのProductVersion / FileVersionはいずれも`0.0.0.2`です。型検査・書式検査も成功しています。

検証日: 2026-09-19。MVPの実装を作成し、実機で検証中です。ここでの「確認済み」は記載した条件の結果であり、すべてのVRM・ディスク・表示環境での動作保証ではありません。未実施を合格として扱いません。

利用者の指定により、2時間連続表示試験は後回しとし、現在の実装成果を引き渡します。検証用アプリは終了済みで、表示試験を自動再開しません。NF-05のメモリ仮目標超過とNF-07の長時間未確認は残課題として保持します。

## 環境と固定バージョン

| 項目 | 実測／採用値 |
| --- | --- |
| OS | Windows 11 x64 |
| CPU | x64ノートPC |
| RAM | 33,599,221,760 bytes（約32GiB） |
| GPU | 8GB級NVIDIA GPU |
| NVIDIAドライバー | 検証時の対応版 |
| 実装 | Electron 44.4.3、Three.js 0.180.0、@pixiv/three-vrm 3.5.5、Zod 4.6.5 |
| ビルド | Node.js 24.20.0、esbuild、Windows .NET Framework C#コンパイラ |
| 推論モデル | ユーザー提供の9B級GGUF |
| GGUF SHA-256 | ローカル検証記録で照合（公開文書には記載しない） |
| 既存サーバー | b9843 / commit 86b94708f、127.0.0.1:8080、起動時context 16384・並列4 |
| アプリ設定 | context 4096、通常output 512、測定用短文128、thinking無効 |
| 更新ランタイム | b10964 / commit b29c606e2、公式安定v0.4.1対応、Windows CUDA 13.3 x64 |

CPU/RAM/GPU/ドライバーはCIMとnvidia-smiで確認しました。電源モード、AC接続状態、室温、他アプリ負荷を固定した専用ベンチマーク室ではありません。

更新ランタイムはローカル検証領域へ別置きし、公式アセットのSHA-256を検証しました。別ポート・CPUモードで同GGUFの6意図判定と日本語ストリームを確認し、検証後にその専用プロセスを終了しました。`--list-devices` でNVIDIA GPUの認識も確認済みです。既存接続先の入替えはしていません。[公式リリースv0.4.1](https://github.com/ggml-org/llama.cpp/releases/tag/v0.4.1)

依存の正確な解決結果は `package-lock.json` に固定しています。`npm audit` の取得結果は脆弱性0件でした（取得時点。将来の脆弱性がないことを保証するものではありません）。

## 確認済みの経路

最終の型チェックとビルドは成功。自動テストは96件中95件成功・失敗0件・スキップ1件です。スキップはこのD:ボリュームで8.3短縮名を作れない場合の実短縮パス試験で、正規化の模擬試験と実ジャンクション拒否は別に通しています。

単体・結合テストでは、分類・入力境界、ルート保護、実NTFSでの移動と復元、ID/ハッシュ照合、同名競合、ハードリンク・ジャンクション拒否、承認失効、ハッシュ中断、現行1操作後の停止、記録故障、クラッシュ相当の結果保存前故障、手動照合、SQLiteバックアップと破損復元を確認しています。

LLM通信はUTF-8/SSEの分割、途中切断、解析不正、出力上限、明示中断・タイムアウトによる通信解放、テンプレート込みのtoken計数と履歴削減、旧APIへの限定fallbackを模擬サーバーで検証しています。Renderer/Mainの回帰テストでは、モデル切替の非同期競合、非表示、入力・IME保持、状態表示、設定中の再描画、処理例外後の解除を確認しています。

Playwrightで実Electronを起動して以下を通しました。テスト対象は専用のフィクスチャであり、個人データを移動していません。

- 設定→日本語ストリーミング→会話から整理案→検証→承認→実移動→復元。
- 自作VRM 1.0/0.xのインポート、同モデルの再選択と次のインポート。
- 推論の停止とサーバー接続の終了、応答上限時の部分本文とエラー表示。
- バックアップ、再起動後のモデル・設定・会話の復元、個別会話削除と全履歴削除。
- rendererのNode無効化、直接通信の拒否、外部LLM endpointの拒否。
- 配布用EXEの独立保存先でも、上記の会話・VRM・再起動・保存の経路。
- パネル上部でアバターだけを非表示・再表示し、モデル選択と会話を維持。非表示状態の再起動後の保持。処理中の表示切替と右ボタン操作は回帰試験でも確認。

公式three-vrm v3.5.5の `VRM1_Constraint_Twist_Sample.vrm` でも、MToon、14個のconstraint、22個のspring、36,470三角形、19画像（展開85.25MiB）の表示・切替・再起動・原本ハッシュ保持を確認しました。全身が収まるよう、姿勢反映後のboundsからカメラ位置を計算します。検証素材は`.local`だけに保存し、配布物には含めません。モデルの探索・選定は引き続きユーザーに委ねます。[公式サンプル](https://github.com/pixiv/three-vrm/blob/v3.5.5/packages/three-vrm/examples/models/VRM1_Constraint_Twist_Sample.vrm)

現在の125% DPIでは、自分の検証用ウィンドウに対するWin32 `WindowFromPoint` とスタイル照合により、モデル上の入力・背面への透過・透過解除を確認しました。他アプリへの入力、100/150/200% DPI、モニター着脱の代替試験とはしていません。

## LLMの意図判定と応答速度

既存8080のモデル読込み済みサーバーを使い、アプリ側context 4096、通常応答の上限128 tokens、thinking無効で測定しました。意図判定はtemperature 0、通常応答は0.7です。モデル・サーバー構成を変更せず、テスト用の文章だけを1件ずつ送り、ケース間を300ms空けました。ファイル操作は実行していません。

固定20問の期待値は最初の測定前に保存し、プロンプト調整後も変更していません。この20問は調整に使用した評価セットです。別途、未使用の10問（holdout）の期待値を最初の問い合わせ前に固定し、その結果を使った追加調整はしていません。

| 意図判定の指標 | 最終結果 |
| --- | ---: |
| 固定20問の `intent / method / target` 完全一致 | 18/20（90%、誤り10%） |
| 未使用10問の同完全一致 | 9/10（90%、誤り10%） |
| 未対応操作を `organize` とした誤判定 | 0/8件（固定セット4件＋holdout 4件） |

未対応操作には削除、リネーム、内容変更、再帰整理、意味による分類を含めています。0/8はこの入力集合での結果であり、任意の自然文で誤判定がないことを保証しません。残る誤りは次の3件です。

| ケース | 本来の分岐 | 実際の分岐 |
| --- | --- | --- |
| I07: 選択中とは別のフォルダを種類別に整理 | `organize / by_extension / other` | `unsupported / null / other` |
| I10: 名前を指定した別フォルダを片付ける（方式なし） | `clarify / null / other` | `unsupported / null / other` |
| H02: 対象は後で選び、ファイルを種類別に分ける | `organize / by_extension / unspecified` | `clarify / null / unspecified` |

いずれも未対応の説明または追加確認へ進む誤分類です。自然文だけで実行を承認することはなく、明示的な整理ボタン、対象選択、計画プレビュー、一括承認を維持しています。会話による依頼が常に正しく解釈される状態には達していません。

holdoutの意図判定では、実テンプレートの計数＋出力予約192 tokensが最大668 tokens、旧API用の保守的なUTF-8バイト推定＋出力予約が最大3872で、いずれも設定4096以内でした。旧API fallbackの既存テストも通過しています。この値は当該短文ケースのものであり、長文入力の上限を保証するものではありません。

最終プロンプトで固定10個の短いチャットも測定しました。10/10件が通常会話へ分岐し、空でない本文を返して `finish_reason=stop` で終了しました。この成功数は通信・経路・未実行のファイル作業を完了したと主張しないことの確認であり、文章の自然さや事実の正確さを満点と評価したものではありません。

| 指標 | 件数 | 最小 | 中央値 | p95 | 最大 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 意図判定の所要時間 | 20 | 422.90ms | 480.30ms | 590.33ms | 1259.49ms |
| 意図判定開始→本文最初のdelta | 10 | 896.73ms | 1350.25ms | 1632.96ms | 1641.56ms |
| サーバー報告の生成速度 | 10 | 37.12 token/s | 49.88 token/s | 53.96 token/s | 54.89 token/s |

本文までの総時間には、意図判定、テンプレート適用、トークン計数、待ち時間を含みます。生成速度はSSEの `timings.predicted_per_second` を使用し、本文に現れない終了トークン等を含み得るサーバー側の定義です。本文を終了後に `/tokenize`（`add_special=false / parse_special=false`）で再計数した参考速度は中央値49.86 token/sでした。この参考値は本文トークン数を最初から最後のdeltaまでの時間で割り、最初のchunkも分子に含めるため、短文では厳密なデコード速度と異なります。再計数の通信時間は速度・初回応答時間に含めていません。p95は各ケースの値を線形補間して算出しています。

この10件ではNF-04の暫定目標（初回本文まで5秒以内、生成15 token/s以上）を満たしました。コールド起動、長い履歴、別モデル、CPUのみの速度はこの測定から判断しません。サーバーの排他利用やキャッシュ状態、他アプリ負荷も固定していません。実行時の記録は毎分短文を依頼する耐久試験との並行を前提としていましたが、後述の停止時刻は18:31 JST、最終LLM測定は18:44:10〜18:44:50 JSTです。その停止済みプロセスとの同時推論があったとは扱わず、専有条件を確認したベンチマークとも扱いません。

## 描画・応答性の測定

自作の軽量VRM 1体、380×540の透明ウィンドウ、設定上限30fps。30秒ウォームアップ後、推論なし5分・既存8080への短文会話15依頼を含む5分を測りました。WebGLの色バッファclear数を描画フレームの近似とし、rAF呼出数とは分離しました。

| 指標 | 推論なし5分 | 推論あり5分 |
| --- | ---: | ---: |
| 描画近似fps | 28.80 | 28.78 |
| フレーム間隔p95 | 35.5ms | 35.5ms |
| UI問い合わせ往復p95 | 1.98ms | 2.18ms |
| アプリ全プロセスworking set最大 | 654.2MiB | 663.8MiB |

推論中だけのUI問い合わせp95は2.15msでした。実クリックから画面描画までの遅延ではなく、Playwrightの評価呼び出しの往復です。NF-03の実操作p95を正式に測った値とは区別します。推論中の最大フレーム間隔は166.7msでした。

非表示の3秒間は、`document.hidden=false` の条件でもMainの `visible:false` によりWebGL clear/draw増分0を確認しました。rAF自体は動作していたため、明示的な停止制御を検証できています。[ElectronのPage Visibility仕様](https://www.electronjs.org/docs/latest/api/browser-window#page-visibility)

15依頼中14件は応答完了、最後の1件は計測終了時に明示キャンセルしました。数値計測後のスクリーンショット取得だけがタイムアウトしたため、この実行の終了コードは1です。数値と通信結果は保存済みで、LLM・rendererの測定中エラーはありません。

初期ビルドの2時間表示・会話・モデル切替試験は、約38分で終了しました。記録は `completed=false`、経過2,321,468ms（約38.7分）で終了し、`Target page, context or browser has been closed` を記録しています。WindowsのApplication Hang（イベント1002）のPID・開始時刻が試験プロセスと一致し、同一レポートIDのWindows Error Reporting（イベント1001）で `AppHangB1` を確認しました。手動終了の有無はこの実行について確定しておらず、終了原因の断定はしません。NF-07の2時間完走は未確認です。

同試験のアプリ全プロセスworking setの観測最大値は1816.46MiBでした。LLMを除くアプリのメモリ合計1GB以内というNF-05の仮目標を超過しており、NF-05も未合格です。これは各サンプル時点のworking set合計の最大値であり、プロセス別の過去ピーク値を加算した値ではありません。共有ページを重複計上し得る指標で、専用VRAM使用量ではありません。メモリ超過と終了の因果関係は確定していません。

19:01の配布版の再試験も約83秒で終了しましたが、ユーザーから「ウィンドウが邪魔で、表示だけを停止する操作が見つからず手動終了した」と確認できました。この試験は外部操作による中断であり、終了コードやWERだけで自発的クラッシュとは判定しません。次の試験も表示操作の改善を優先して担当エージェントが中断しました。最初の約38分の終了については個別の操作状況を確定していません。2時間完走の判定は引き続き未完了です。

表示操作の修正後、配布EXEで非表示・再表示・再起動後の非表示保持まで通過しました。途中の試験1回では起動順の差によりテストが隠れたアバター画面を会話パネルと誤認してタイムアウトしたため、URLで対象パネルを特定するハーネスへ修正し再試験しました。19:17 JSTから再開した試験は約65秒でアバターの非表示を検出し、再表示せず中断しました。保存済みの`avatarVisible=false`も確認済みです。表示だけを止める新しい導線が機能しており、以後の表示試験は自動再開していません。

別の100回連続切替は約66秒でエラー・クラッシュ・応答停止0件、正常終了でした。working set合計は初期789MiB、最大1122.7MiB、待機10秒後679MiB。WebGL texture/buffer/programの作成数−削除数は同じモデルの初回と最終回で一定でした。この短い試験で持続的増加は確認できませんでしたが、1GB目標の超過は残り、2時間試験を代替しません。

模擬SSEを受信中の実Playwright入力25操作では、trustedイベント受信から期待DOM反映後のrAFまでのp95は入力5.1ms（10件）、パネル切替10.7ms（10件）、停止13.6ms（5件）でした。全操作で処理中の状態を確認し、5本のストリームは停止により終了しました。これは画面提示完了や実LLMのGPU負荷下の遅延ではなく、限定条件でのDOM応答測定です。

## 要件対応

| 要件 | 状況と評価範囲 |
| --- | --- |
| AV-01/02 | 両世代のフィクスチャと公式MToon/springサンプルで表示・切替・コピー原本保全・再起動を検証。100回切替も成功 |
| AV-03/04 | 125% DPIの透明表示とクリック透過、明示的な非表示・再表示を検証。DPI混在、モニター着脱、スリープ復帰は未確認 |
| AV-05 | 表情なしの軽量モデルでも会話・整理が動作。状態はパネルに表示 |
| CH-01〜04 | 実サーバーの日本語応答、模擬障害、停止・再入力・履歴と人格設定を確認 |
| CH-05 | 会話から計画・対象確認への接続を確認。最終固定20問18/20、未使用10問9/10一致。未対応→organizeは0/8。追加確認・未対応側への誤分類が残る |
| AG-01〜08 | 実NTFSと故障注入による主要保全ケース、復旧・復元の読取り予算と競合、保護パス正規化を確認。大容量の予算境界は模擬サイズ |
| ST-01 | 保存先表示、整合性確認、明示バックアップ、原本保管と承認失効付き復元を検証 |
| ST-02 | 起動時常駐は初期無効、所有アプリ終了・外部LLM存続を確認。Windowsログイン実行は未確認 |
| NF-01 | renderer外向き通信を遮断し、MainのLLM接続はloopback限定。物理的なネットワーク切断試験は未実施 |
| NF-02/03 | 約29fps・非表示描画停止とUI問い合わせ応答を確認。高負荷のユーザーVRM、実クリック遅延は別確認 |
| NF-04 | 最終固定10短文で初回本文最大1.642秒、生成最小37.12 token/s。本測定条件では暫定目標内。コールド起動・長文・別モデル・CPUのみは未確認 |
| NF-05 | 未合格。初期ビルドでworking set最大1816.46MiB、修正版100回切替では1122.7MiB。待機後低下するが1GBの仮目標は超過 |
| NF-07 | 未完了。19:01はユーザーの手動終了、19:17開始の試験は非表示操作を検出して中断。2時間表示の連続完走は未確認 |
| NF-06 | 専用テストで未承認移動・上書き・許可外変更0件。実電源断、ディスク物理障害は未実施 |
| NF-08/09 | モジュール分離、会話保存無効化、用途限定IPC、ファイル名・本文を分類LLMへ送らない構成を確認 |

## 再実行と証跡

```powershell
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
npm.cmd run test:ui
node scripts/acceptance.mjs
node scripts/benchmark-ui.mjs
node --import tsx scripts/benchmark-llama.ts
node scripts/endurance.mjs
```

配布物の結合テストは `COMPANION_PACKAGE` にEXEのパスを指定して `scripts/acceptance.mjs` を実行します。すべての証跡はGit管理外の `artifacts` にあります。

| 証跡 | 対象 |
| --- | --- |
| `artifacts/ui-1789809068182/` | 会話→整理→復元とVRM両世代の実UI |
| `artifacts/acceptance-1789810296755/` | ソース版の停止・再起動・バックアップ |
| `artifacts/acceptance-1789810472208/` | 配布EXEで同経路 |
| `artifacts/benchmark-ui-1789809449345/result.json` | 5分+5分の描画・UI・メモリ測定 |
| `artifacts/llama-verification.json` | 既存GPUサーバーの基本接続確認 |
| `artifacts/llama-b10964-cpu-verification.json` | 更新版CPUサーバーの互換性確認 |
| `artifacts/llama-benchmark-1789810586857/` | 固定20問/10プロンプトの初回評価、期待値の事前固定記録 |
| `artifacts/llama-benchmark-1789811050603/manifest.json` | 最終測定の固定20問・10短文、期待値、条件、LLMアダプターのSHA-256 |
| `artifacts/llama-benchmark-1789811050603/results.json` | 最終固定20問18/20一致、10短文の本文・生のtimings・初回応答時間・集計 |
| `artifacts/llama-holdout-1789811183889/manifest.json` | 未使用10問の問い合わせ前に保存した期待値と条件 |
| `artifacts/llama-holdout-1789811183889/results.json` | 未使用10問9/10一致、未対応→organize 0/4、実トークン数と旧API推定予算 |
| `artifacts/endurance-1789807978939/result.json` | 初期ビルドの試験未完了、約38.7分で終了、working set最大1816.46MiB |
| `artifacts/endurance-1789807978939/windows-events.json` | 同試験プロセスのApplication Hang 1002／WER 1001・AppHangB1、メッセージとXML |
| `artifacts/npm-audit.json` | 依存監査結果 |
| `artifacts/unit-tests.tap` | 最新の単体・結合・回帰試験 |
| `artifacts/ui-1789812068694/` | 最終の会話→整理→復元の実UI |
| `artifacts/acceptance-1789812729887/` | 表示・非表示と再起動保持を追加したソース版検証 |
| `artifacts/acceptance-1789812983699/` | 表示操作を改善した配布EXEで非表示・再表示・再起動保持を含む経路が成功 |
| `artifacts/acceptance-1789812011960/` | 表示操作追加前の配布版の全経路 |
| `artifacts/verify-avatar-1789811437228/` | 公式サンプルの全身表示と125% DPIでの透過検証 |
| `artifacts/stress-avatars-1789811758467/` | 公式・自作モデルの100回切替 |
| `artifacts/responsiveness-1789812338388/` | 模擬ストリーム受信中の25実入力操作 |
| `artifacts/endurance-1789812033538/` | ユーザーによる手動終了を確認した再試験 |
| `artifacts/endurance-1789812262471/` | 表示操作改善のため担当エージェントが中断した再試験 |
| `artifacts/endurance-1789813054785/` | 表示操作改善後の配布EXE。非表示を検出して約65秒で中断し、勝手に再表示しないことを確認 |

耐久試験開始後にモデル切替・保存故障等を修正したため、初期ビルドの不合格結果と修正版の再試験を区別します。NF-05/07の合格は未確認で、MVP全必須要件の合格・v0.1の完成を宣言しません。修正点は別の回帰・実UI・性能試験で確認します。コード署名、一般配布用インストーラー、自動更新は個人利用MVPの対象外です。


## v0.0.0.4 入力操作修正（2026-09-20）

- 型検査・書式検査成功。単体テスト113件中112成功、既存環境条件によるスキップ1件。
- 修正前の配布版で、長文通知によるクリック遮断、状態通知でのUndo履歴消失、IME 229 Enterの送信条件を再現。調査詳細は [入力調査](input-investigation-2026-09-20.md)。
- `node scripts/investigate-input.mjs --verify` を新配布EXEに実行し成功。状態更新とIME確定後も同一入力DOMを維持し、Ctrl+Zで更新前まで戻れる。通知表示中の座標クリックで入力できる。通常Enter送信は1回、Shift+Enterは改行、設定欄のDOMとUndoも維持。証跡: `artifacts/input-investigation-1789879094591/report.json`。
- 新配布EXEで既存UIスモーク成功: SSE会話、会話から整理案、承認、移動、復元、VRM 0.x/1.0インポート。証跡: `artifacts/ui-1789879100097/`。
- Playwright/CDPの検証であり、実Windows IME、ネイティブウィンドウ重なり、通知のない起動直後の入力不能の完全解消は未確認。ユーザーの実データは使わず、専用user-data-dirとモックサーバーで実施。


## v0.0.0.5 失敗時・停止時の修正（2026-09-20）

- 型検査・書式検査成功。単体テスト124件中123成功、既存NTFS短縮名条件のスキップ1件。会話保存失敗、VRM登録失敗後のコピー削除、フォルダ登録と履歴削除の停止、失敗ターン除外、後続GGUF分割ファイル変更、不正VRMメタデータ、送信拒否と次の下書き保持の回帰を追加。最終main変更後もライフサイクル全17件成功。
- 新配布EXEの入力回帰: `artifacts/input-investigation-1789892993622/report.json`。既存のIME/CDP・Undo・通知中の入力に加え、APIキー保存後のクリア、削除後の再登録、受理前の送信拒否で下書きを保持することを確認。
- 新配布EXEのUIスモーク成功: `artifacts/ui-1789892998286/`。会話、整理案、承認、移動、復元、VRM 0.x/1.0。
- 新配布EXEの受け入れ9チェック成功: `artifacts/acceptance-1789892998812/`。ネットワーク制限、SSE停止、出力上限、アバター再選択・表示、バックアップ、再起動時の状態保持、個別・全履歴削除。
- 検証は専用データ・ダミーキー・モックLLMを使用。分割GGUFは軽量子プロセス、保存障害は依存の障害注入。実推論・実Windows IME・透明ウィンドウ重なりの包括的検証とは区別する。既に完了したバックアップ内の削除やOS権限によるVRMコピー削除失敗は巻き戻さない。
